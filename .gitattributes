# scripts/*.sh corren en el servidor (Linux) vía shebang: un CRLF en la
# primera línea rompe el intérprete con "bash^M: No such file or directory".
# Sin esto, un checkout en Windows con autocrlf activado los convertiría.
scripts/*.sh text eol=lf
